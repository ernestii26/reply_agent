從以下貼文中提取關鍵字，用於搜尋相關討論：

標題：{title}
內容：{content}

請提取最重要的1-3個關鍵字（如：科系名稱、大學名稱、分數範圍等），用空格分隔。
只回傳關鍵字，不要解釋。
從以下貼文中提取關鍵字，用於搜尋相關討論：

標題：{title}
內容：{content}

請提取最重要的1-3個關鍵字（如：科系名稱、大學名稱、分數範圍等），用空格分隔。
只回傳關鍵字，不要解釋。
